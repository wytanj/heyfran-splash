import { teaser } from '../shared.js';

/** 0 - cream-wordmark. Cream field, yellow wordmark, fortune lede. */
export function mount(root) {
  teaser(root, {
    tone: 'yellow',
    eyebrow: 'heyfran',
  });
}
